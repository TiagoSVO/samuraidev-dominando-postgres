SELECT 'DESPESA' AS "REGISTRO", 
		SUM(VALOR) AS "VALOR", 
		DESCRICAO AS "CATEGORIA"
FROM LANCAMENTO
WHERE VALOR < 0
GROUP BY DESCRICAO
UNION
SELECT 'LUCRO' AS "REGISTRO", 
		SUM(VALOR) AS "VALOR", 
		DESCRICAO AS "CATEGORIA"
FROM LANCAMENTO
WHERE VALOR >= 0
GROUP BY DESCRICAO
ORDER BY "VALOR";