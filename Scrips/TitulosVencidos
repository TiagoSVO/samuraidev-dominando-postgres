SELECT CL.NOME_EMPRESARIAL AS "NOME DO CLIENTE", 
	   FO.NOME_EMPRESARIAL AS "NOME DO FORNECEDOR", LA.*
FROM LANCAMENTO LA
LEFT JOIN FORNECEDOR FO
ON FO.ID = LA.ID_FORNECEDOR
LEFT JOIN CLIENTE CL
ON CL.ID = LA.ID_CLIENTE
WHERE DATA_VENCIMENTO < NOW()
AND LA.SITUACAO != 'PAGO';